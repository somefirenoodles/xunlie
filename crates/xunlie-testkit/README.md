# xunlie-testkit

Shared test-only utilities for Xunlie crates and external adapter conformance
suites. It supplies disposable workspaces, fixture path normalization and JSON
schema assertions. Production crates must not depend on this package.
